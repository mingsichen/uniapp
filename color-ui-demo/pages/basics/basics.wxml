<scroll-view scroll-x class="bg-white nav text-center fixed" style="top:{{CustomBar}}px">
	<view class="cu-item {{index==TabCur?'text-blue cur':''}}" wx:for="{{tabNav}}" wx:key bindtap='tabSelect' data-id="{{index}}">
		{{tabNav[index]}}
	</view>
</scroll-view>

<block wx:if="{{TabCur==0}}" >
	<view class="cu-bar bg-white solid-bottom margin-top" style="margin-top:110rpx">
		<view class="action">
			<text class="cuIcon-title text-blue"></text>固定尺寸
		</view>
	</view>
</block>

<block wx:if="{{TabCur==1}}">
	<view style="margin-top:110rpx">22</view>
</block>
<block wx:if="{{TabCur==2}}">
	<view style="margin-top:110rpx"> 33</view>

</block>